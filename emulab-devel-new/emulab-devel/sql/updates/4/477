use strict;
use libdb;

sub DoUpdate($$$)
{
    my ($dbhandle, $dbname, $version) = @_;

    DBQueryFatal("REPLACE INTO table_regex VALUES ".
		 " ('sitevariables','value','text','redirect', ".
		 "  'default:html_text',0,0,NULL)");
    return 0;
}

# Local Variables:
# mode:perl
# End:
