#
# The template
#
use strict;
use libinstall;
use installvars;

sub Install($$$)
{
    my ($server, $isupdate, $impotent) = @_;

    # Replace if this script does an update for ip/domain.
    return 0
	if ($isupdate);

    Phase "foo", "Checking foo", sub {

    };
    return 0;
}

# Local Variables:
# mode:perl
# End:
